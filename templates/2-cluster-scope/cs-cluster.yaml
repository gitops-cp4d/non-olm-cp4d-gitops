apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cs-cluster-{{ .Values.global.appSuffix }}
  namespace: {{ .Values.global.argocdNS }}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: {{ .Values.global.helmRepoURL }}
    chart: {{ index .Values.apps "cs-cluster" "chart" }}
    targetRevision: {{ index .Values.apps "cs-cluster" "version" }}
    helm:
      valuesObject:
        global:
          operatorNamespace: {{ .Values.global.operatorNS }}
  project: default
