apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: im-cluster-{{ .Values.global.appSuffix }}
  namespace: {{ .Values.global.argocdNS }}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: {{ .Values.global.helmRepoURL }}
    targetRevision: {{ index .Values.global.apps "im-cluster" "version" }}
    chart: {{ index .Values.global.apps "im-cluster" "chart" }}
    helm:
      valuesObject:
        global:
          operatorNamespace: {{ .Values.global.operatorNS }}
  project: default
