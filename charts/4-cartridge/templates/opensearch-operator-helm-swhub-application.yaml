apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opensearch-operator-helm-{{ .Values.global.appSuffix }}
  namespace: {{ .Values.global.argocdNS }}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: {{ .Values.global.helmRepoURLdev }}
    targetRevision: main
    path: repos
    chart: opensearch
    helm:
      releaseName: opensearch-operator-helm
      valueFiles:
        - ../../values.yaml
  project: default
