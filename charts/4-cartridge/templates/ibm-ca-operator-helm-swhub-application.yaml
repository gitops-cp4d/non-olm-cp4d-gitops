apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ibm-ca-operator-helm-{{ .Values.global.appSuffix }}
  namespace: {{ .Values.global.argocdNS }}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: {{ .Values.global.helmRepoURLdev }}
    targetRevision: main
    path: repos
    chart: ibm-ca-operator-helm
    helm:
      releaseName: ibm-ca-operator-helm
      valueFiles:
        - ../../values.yaml
  project: default
