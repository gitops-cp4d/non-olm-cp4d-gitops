---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement-key
  namespace: ${operatorNS}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: |
    {
      "auths": {
        "cp.icr.io": {
          "username": "cp",
          "password": "${entitlementKey}",
          "email": "cpd@ibm.com",
          "auth": ""
        }
      }
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement-key
  namespace: ${instanceNS}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: |
    {
      "auths": {
        "cp.icr.io": {
          "username": "cp",
          "password": "${entitlementKey}",
          "email": "cpd@ibm.com",
          "auth": ""
        }
      }
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement-key
  namespace: ${licensingNS}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: |
    {
      "auths": {
        "cp.icr.io": {
          "username": "cp",
          "password": "${entitlementKey}",
          "email": "cpd@ibm.com",
          "auth": ""
        }
      }
    }
